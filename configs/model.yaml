defaults:
  - _self_

model:
  backbone: "DeepPavlov/rubert-base-cased"
  num_classes: 3
  dropout: 0.1

  learning_rate: 2e-5
  weight_decay: 0.01
  warmup_steps: 100

  freeze_backbone: false
  num_frozen_layers: 0
